import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    root: {
        width: '70vw',
        minHeight: '80vh',
        borderRight: '2px solid black',
    },
    tabs: {
        flexGrow: 1,
    },
    centered: {
        textAlign: 'center',
        marginTop: '2rem',
        marginBottom: '1ren',
    }
})